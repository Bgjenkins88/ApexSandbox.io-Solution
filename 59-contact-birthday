public Boolean isBirthday(Contact c) {
    if(c.birthdate == null) {
        return false;
    }
    Date today = Date.today();
    return (today.month() == c.birthdate.month() && today.day() == c.birthdate.day());
}
