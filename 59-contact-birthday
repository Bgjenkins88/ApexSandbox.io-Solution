public Boolean isBirthday(Contact c) {
    if(c.birthdate == null) {
        return false;
    }
    String todayDate       = Date.today().format('M/d');
    String contactBirthday = c.birthdate.format('M/d');
    return todayDate == contactBirthday;

}