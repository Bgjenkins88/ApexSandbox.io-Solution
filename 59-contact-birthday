public Boolean isBirthday(Contact c) {
   if (contactBirthday == null) {
    return false;
}
String todayMonthDay = Date.today().format('M/d');
String birthdayMonthDay = contactBirthday.format('M/d');
return todayMonthDay == birthdayMonthDay;
}