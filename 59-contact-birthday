public Boolean isBirthday(Contact c) {
    //code here
    String today           = date.today().format('M/d');
    String contactBirthday = c.birthdate.format('M/d');

    return today == contactBirthday? True : False;
}